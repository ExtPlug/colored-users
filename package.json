{
  "name": "extplug-colored-users",
  "version": "1.0.0",
  "license": "MIT",
  "description": "ExtPlug plugin that gives everyone a mostly-unique color.",
  "main": "build/colored-users.js",
  "keywords": [
    "extplug-plugin"
  ],
  "files": [
    "build",
    "src"
  ],
  "dependencies": {
    "onecolor": "^3.0.4"
  },
  "devDependencies": {
    "extplug-cli": "^1.0.0"
  },
  "scripts": {
    "prepublishOnly": "npm run build -- --minify",
    "build": "extplug bundle src/main.js build/colored-users.js"
  }
}
